{
    "name": "scrape-tools",
    "version": "1.0.0",
    "description": "",
    "main": "scrape.js",
    "scripts": {
        "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
        "start": "npm run build && node dist/scrape.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "axios": "^0.17.0",
        "babel-polyfill": "^6.26.0",
        "cheerio": "^1.0.0-rc.2",
        "rimraf": "^2.6.2",
        "striptags": "^3.1.0"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-env": "^1.6.1"
    }
}
